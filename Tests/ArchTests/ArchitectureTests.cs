using Xunit;

namespace ThunderPropagator.ArchTests
{
    public class ArchitectureTests
    {
        [Fact]
        public void Baseline_Passes()
        {
            // Simple baseline to ensure the ArchTests project compiles and runs.
            Assert.True(true);
        }
    }
}
